<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Equipo de Desarrollo</title>
    <!-- Incluir Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <!-- CSS Personalizado para fondo oscuro y colores (asumo que existe) -->
    <link rel="stylesheet" href="/static/colores.css"> 
    <style>
        /* Estilos generales para el fondo y la tipografía */
        body {
            background-color: #2f2f2f;
            color: #fff;
            padding: 20px;
        }
        /* Título principal */
        h1 {
            color: #00b4d8; /* Un color vibrante para el título */
            font-weight: bold;
            text-align: center;
            margin-bottom: 5px;
        }
        /* Subtítulo */
        .subtitle {
            text-align: center;
            margin-bottom: 40px;
            font-size: 1.1rem;
        }

        /* CARD DE PROGRAMADOR */
        .programmer-card {
            background-color: #3f3f3f; 
            color: #fff;
            border: none;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
            height: 100%; /* Asegura que todas las tarjetas tengan la misma altura */
        }
        .programmer-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }
        .programmer-card img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
            border: 3px solid #00b4d8;
        }
        .programmer-card h4 {
            font-weight: 600;
            color: #fff;
            margin-bottom: 5px;
        }
        .programmer-card p {
            color: #ccc;
            font-size: 0.95rem;
        }
        .programmer-card a {
            color: #03ff9e; /* Color de acento para enlaces */
            text-decoration: none;
            font-weight: 500;
            display: block;
            margin-top: 10px;
        }

        /* CONTENEDOR FLEX PARA LA DISPOSICIÓN HORIZONTAL */
        .team-container {
            display: flex;
            flex-wrap: wrap; /* Permite que las tarjetas salten a la siguiente fila en pantallas pequeñas */
            justify-content: center; /* Centra las tarjetas horizontalmente */
            gap: 30px; /* Espacio entre las tarjetas */
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Media Query para asegurar que las tarjetas se apilan en dispositivos pequeños */
        @media (max-width: 768px) {
            .team-container {
                flex-direction: column; /* Apila las tarjetas verticalmente */
                align-items: center;
            }
            .programmer-card {
                width: 80%; /* Ancho apropiado para móvil */
                max-width: 350px;
            }
        }
        
        /* Estilo para los elementos individuales dentro del contenedor flex */
        .team-member {
            flex: 0 0 auto; /* No crecerá ni encogerá, usará el ancho de su contenido */
            width: 300px; /* Ancho fijo para cada tarjeta en escritorio */
        }
        
    </style>
</head>
<body>
    <div class="container-fluid">
        <!-- Título y descripción -->
        <h1 class="mt-4">Equipo de Desarrollo</h1>
        <p class="subtitle">Conoce a los programadores detrás de este Sistema de Gestión de Bases de Datos.</p>

        <!-- CONTENEDOR FLEXBOX PARA LAS TARJETAS -->
        <!-- Las tarjetas se mostrarán de izquierda a derecha -->
        <div class="team-container">

            <!-- PROGRAMADOR 1 -->
            <!-- Nota: En un EJS real, esto estaría en un bucle sobre un arreglo de programadores -->
            <div class="team-member">
                <div class="programmer-card">
                    <!-- Asumo que tienes la ruta a la imagen del programador -->
                    <img src="URL_IMAGEN_DIEGO" alt="Foto de Diego Aaron García Casillas">
                    <h4>Diego Aaron García Casillas</h4>
                    <p>Líder de Proyecto / Backend</p>
                    <a href="mailto:diego@utec.edu.mx">diego@utec.edu.mx</a>
                </div>
            </div>

            <!-- PROGRAMADOR 2 -->
            <div class="team-member">
                <div class="programmer-card">
                    <img src="URL_IMAGEN_EDUARDO" alt="Foto de Eduardo Gomez Escobar">
                    <h4>Eduardo Gomez Escobar</h4>
                    <p>Desarrollador Frontend</p>
                    <a href="mailto:programador@utec.edu.mx">programador@utec.edu.mx</a>
                </div>
            </div>
            
            <!-- PROGRAMADOR 3 (Si hay más, se agregarían aquí) -->
            <div class="team-member">
                <div class="programmer-card">
                    <img src="URL_IMAGEN_TERCERO" alt="Foto de Tercer Miembro">
                    <h4>Tercer Miembro Ejemplo</h4>
                    <p>Documentación y QA</p>
                    <a href="mailto:tercero@utec.edu.mx">tercero@utec.edu.mx</a>
                </div>
            </div>

        </div>
        
        <!-- Botón de retorno al final (Asumo que esta parte de la interfaz existe) -->
        <div class="text-center mt-5">
             <a href="/" class="btn btn-info btn-lg">Volver al Inicio</a>
        </div>
        
    </div>

    <!-- Asumo que tienes un archivo footer.ejs -->
    <%- include('footer.ejs') %>
</body>
</html>