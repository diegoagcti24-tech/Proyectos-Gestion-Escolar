<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plataforma de Gestión Escolar</title>
    <!-- Incluir Bootstrap CSS (aunque estás usando CSS nativo, es bueno tenerlo si usas clases de utilidad) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" xintegrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Incluir el CSS personalizado para el fondo y colores -->
    <link rel="stylesheet" href="/static/colores.css">
    
    <style>
        /* Estilos adaptados del diseño que proporcionaste */
        body {
            font-family: sans-serif;
            margin: 0;
            background-color: #2f2f2f; /* Fondo oscuro */
            color: #fff; /* Color de texto blanco */
            /* IMPORTANTE: Damos al body una altura mínima para que el hero-section pueda usar 90vh */
            min-height: 100vh; 
            display: flex; 
            flex-direction: column; /* Permite que el header, main y footer se apilen */
        }

        /* Sección con imagen de fondo */
        .hero-section {
            position: relative;
            text-align: center;
            padding: 60px 10px;
            /* RUTA ESTÁTICA DIRECTA: Se asume que /CRUD.jpeg está accesible en la carpeta 'static' */
            background: url("/CRUD.jpeg") no-repeat center center/cover;
            min-height: 90vh; /* Altura mínima para que sea visible (Casi toda la pantalla) */
            flex-grow: 1; /* Ocupa el espacio restante entre header y footer */
            display: flex;
            align-items: center; /* Centrado Vertical */
            justify-content: center; /* Centrado Horizontal */
            flex-direction: column;
        }

        /* Capa oscura encima de la imagen (para visibilidad del texto) */
        .hero-section::before {
            content: "";
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0, 0, 0, 0.5); /* Sombra semitransparente sobre la imagen de fondo */
            z-index: 0;
        }

        /* Contenedor del texto con el recuadro transparente */
        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 800px;
            background-color: rgba(0, 0, 0, 0.6); /* Fondo semi-transparente negro */
            padding: 40px; /* Espaciado interno */
            border-radius: 10px; /* Bordes redondeados */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5); /* Sombra para resaltar */
        }

        .hero-section h1 {
            font-size: 50px;
            margin-bottom: 10px;
            color: #03ff9e; /* Color verde/azul (ajusté el color para que destaque) */
            font-weight: bold;
        }
        .hero-section p {
            font-size: 20px;
            color: #ffffff; /* Asegura que el párrafo también sea blanco para que contraste */
            margin-bottom: 25px; /* Margen para separar del botón */
        }

        /* Estilo del Botón (adaptado al estilo Bootstrap) */
        .btn-primary {
            background-color: #ffc107; /* Amarillo/Naranja de Bootstrap */
            border-color: #ffc107;
            color: #333;
            font-weight: bold;
            padding: 10px 30px;
            font-size: 1.25rem;
            text-decoration: none; /* Quita el subrayado del enlace */
            border-radius: 5px;
            transition: background-color 0.3s ease;
            display: inline-block; /* Permite aplicar padding correctamente */
        }

        .btn-primary:hover {
            background-color: #e0a800; /* Color más oscuro al pasar el ratón */
            border-color: #e0a800;
            color: #333;
        }
    </style>
</head>
<body>
    <%- include('header.ejs', { logged_in: typeof logged_in !== 'undefined' ? logged_in : false }) %>

    <!-- El main ahora es flexible y contiene el hero-section -->
    <main class="flex-grow-1">
        <!-- hero-section ya tiene display: flex, align-items: center y justify-content: center -->
        <section class="hero-section">
            <div class="hero-content">
                <h1>Plataforma de Gestion Escolar</h1>
                
                <p>Accede a la gestión de bases de datos para administrar alumnos, profesores y recursos del sistema.</p>
                
                <a href="/databases" class="btn-primary">
                    ACCEDER A BASES DE DATOS
                </a>
                
            </div>
        </section>
    </main>

    <%- include('footer.ejs') %>
    
    <!-- Incluir Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" xintegrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
